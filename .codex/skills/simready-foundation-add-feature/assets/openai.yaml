interface:
  display_name: "SimReady Add Feature"
  short_description: "Create SimReady features from the canonical template"
  default_prompt: "Use $simready-foundation-add-feature to add a new SimReady Foundation feature from feature-template.md with matching documentation, JSON manifest, requirement mapping, and index updates."
